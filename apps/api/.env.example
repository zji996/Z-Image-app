ENVIRONMENT=dev

API_HOST=0.0.0.0
API_PORT=8000

DATABASE_URL=postgresql+psycopg://z_image:z_image@localhost:5432/z_image
REDIS_URL=redis://localhost:6379/0

# 统一的模型目录，默认可不设置（会回落到 <repo_root>/models）
MODELS_DIR=

CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

