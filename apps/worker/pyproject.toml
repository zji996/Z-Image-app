[project]
name = "z-image-worker"
version = "0.1.0"
description = "Z-Image Celery worker"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "celery>=5.4.0",
    "redis>=5.0.0",
    "python-dotenv>=1.0.0",
]

